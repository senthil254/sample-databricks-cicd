def add_numbers(left: int, right: int) -> int:
    """Return the sum of two integers."""
    return left + right


def normalize_name(name: str) -> str:
    """Normalize a user name for consistent display."""
    return name.strip().title()
